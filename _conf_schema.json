{
  "github_token": {
    "description": "GitHub API访问令牌",
    "type": "string",
    "hint": "可选项。提供GitHub API令牌可以增加API请求限制以及访问私有仓库。格式如: ghp_xxxxxx",
    "obvious_hint": true
  },
  "check_interval": {
    "description": "检查更新间隔时间（分钟）",
    "type": "int",
    "hint": "设置检查GitHub更新的间隔时间，单位为分钟",
    "default": 30
  },
  "use_lowercase_repo": {
    "description": "仓库名使用小写存储",
    "type": "bool",
    "hint": "将仓库名转换为小写进行存储，以避免大小写敏感性问题",
    "default": true
  },
  "auto_resolve_links": {
    "description": "自动解析 GitHub 链接",
    "type": "bool",
    "hint": "是否自动解析群聊中的 GitHub 链接并发送卡片。可通过 /ghlink 指令在特定会话中覆盖此设置",
    "default": true
  },
  "enable_webhook": {
    "description": "启用 GitHub Webhook 模式",
    "type": "bool",
    "hint": "开启后将启动 Webhook 服务并关闭轮询检查",
    "default": false
  },
  "webhook_host": {
    "description": "Webhook 服务监听地址",
    "type": "string",
    "hint": "例如 0.0.0.0 或 127.0.0.1",
    "default": "0.0.0.0"
  },
  "webhook_port": {
    "description": "Webhook 服务监听端口",
    "type": "int",
    "hint": "选择一个对外暴露的端口号",
    "default": 6192
  },
  "webhook_path": {
    "description": "Webhook 请求路径",
    "type": "string",
    "hint": "GitHub Webhook 配置中的路径，需以/开头",
    "default": "/github/webhook"
  },
  "webhook_secret": {
    "description": "Webhook 验证密钥",
    "type": "string",
    "hint": "与 GitHub Webhook 设置中的 Secret 保持一致以验证请求",
    "default": ""
  },
  "codeberg_token": {
    "description": "Codeberg API访问令牌",
    "type": "string",
    "hint": "可选项。提供Codeberg API令牌可以增加API请求限制以及访问私有仓库。",
    "obvious_hint": true
  },
  "enable_codeberg_webhook": {
    "description": "启用 Codeberg Webhook 模式",
    "type": "bool",
    "hint": "开启后将为Codeberg仓库启动 Webhook 服务",
    "default": false
  },
  "codeberg_webhook_path": {
    "description": "Codeberg Webhook 请求路径",
    "type": "string",
    "hint": "Codeberg Webhook 配置中的路径，需以/开头",
    "default": "/codeberg/webhook"
  },
  "codeberg_webhook_secret": {
    "description": "Codeberg Webhook 验证密钥",
    "type": "string",
    "hint": "与 Codeberg Webhook 设置中的 Secret 保持一致以验证请求",
    "default": ""
  },
  "use_ai_summary": {
    "description": "使用 AI 总结 Issue/PR 内容",
    "type": "bool",
    "hint": "启用后，查询 Issue/PR 详情时将使用 AI 对内容进行摘要总结",
    "default": false
  }
}
